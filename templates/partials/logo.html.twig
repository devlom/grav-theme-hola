{% set logo = theme_var('custom_logo') %}
{% if logo %}
  {% set logo_file = (logo|first).name %}
  <img src="{{ url('theme://images/' ~ logo_file)  }}" alt="{{ site.name }}" />
{% else %}
  {% include('@images/logo.png') %}
{% endif %}